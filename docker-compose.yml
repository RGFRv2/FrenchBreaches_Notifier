services:
  notifier:
    build: .
    image: frenchbreaches-notifier:latest
    container_name: frenchbreaches-notifier
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - data:/app/data

volumes:
  data:
